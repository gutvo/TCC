import { type Request, type Response } from 'express'
// import adoptionsService from '../../services/adoptions'
// import translate from '@Dictionary'

export default async function listController (req: Request, res: Response) {
  // try {
  //   const offset = parseInt(req.query.offset as string)
  //   const limit = parseInt(req.query.limit as string)
  //   const ongId = req.query.ongId as string

  //   const { data, pagination, status } = await adoptionsService.listService({ limit, offset, ongId })

  //   return res.status(status).json({ pagination, data })
  // } catch (error) {
  //   res.status(500).json({ message: translate({ id: 'server-error' }) })
  // }
}
