// import { Message } from '@Models/chats/messages'

interface ListServiceProps {
  id: string
}

export default async function listService ({ id }: ListServiceProps) {
  // const data = await Message.findAll({ where: { roomId: id } })

  // return { data }
}
